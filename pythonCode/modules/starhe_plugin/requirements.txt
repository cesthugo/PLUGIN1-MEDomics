# requirements.txt — Plugin STARHE / MEDomics
# Installer avec : pip install -r requirements.txt

# ── Imagerie médicale ─────────────────────────────────────────────────────────
pydicom>=2.4.0
# Décodeurs JPEG / JPEG-2000 / JPEG-LS pour pydicom (requis pour certains DICOM compressés)
pylibjpeg>=2.0.0
pylibjpeg-openjpeg>=2.0.0   # JPEG 2000
pylibjpeg-libjpeg>=2.1.0    # JPEG lossless / lossy

# ── Traitement d'image ────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0.80

# ── IA / Deep Learning ───────────────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
# einops n'est pas importé directement — dépendance transitive de mmdet (installée automatiquement)

# ── Frameworks ML (mmcv-lite = Python pur, pas d'extensions C compilées) ──────
mmengine>=0.10.0
mmcv-lite>=2.0.0rc4,<2.2.0
mmdet>=3.3.0,<4.0.0
# mmaction2 — installé SANS dépendances (mmengine/mmcv/torch déjà présents)
#   pip install mmaction2==1.2.0 --no-deps
# Requis par C3D_BACKEND="mmaction2" dans config.py (défaut).
# Patches venv automatiques appliqués par scripts/setup.sh :
#   localizers/__init__.py  : suppression import DRN absent du wheel
#   roi_heads/__init__.py   : AssertionError ajouté au except (conflit registre mmdet)
#   task_modules/__init__.py: idem
importlib_metadata>=6.0

# ── Base de données ───────────────────────────────────────────────────────────
pymongo>=4.6.0

# ── Interface prototype ───────────────────────────────────────────────────────
Pillow>=10.2.0

# ── Utilitaires ───────────────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0

# ── Pré-traitement prepUS ─────────────────────────────────────────────────────
# prepUS est vendorisé dans third_party/prepUS/ (inclus dans le dépôt).
# run_tkinter.ps1 l'installe automatiquement avec :
#   pip install sonocrop --no-deps
#   pip install ../../../../../../third_party/prepUS --no-deps
# (ou depuis la racine du projet : pip install third_party/prepUS --no-deps)
sonocrop
fire>=0.5.0
rich>=13.0
